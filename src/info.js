import React from "react";

const Info = ({ clicks }) => (
  <p>
    The increaseClicks() function is binded to the class that it is defined in
    (because it is an arrow function).
  </p>
);

export default Info;
